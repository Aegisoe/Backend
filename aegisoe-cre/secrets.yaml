secretsNames:
  - GITHUB_TOKEN            # GitHub Admin token untuk revoke leaked token
  - VAULT_ADMIN_TOKEN       # HashiCorp Vault token untuk rotasi secret
  - BACKEND_CALLBACK_SECRET # Shared secret untuk verifikasi callback CRE -> Backend
  - VAULT_URL               # URL HashiCorp Vault (e.g. https://vault.example.com)
